<h1>投稿一覧</h1>
<% @books.each do |book| %>
  <div>
    <%= image_tag book.get_image %>
    <p>タイトル：<%= book.title %></p>
    <p>説明：<%= book.body %></p>
    <p>ユーザーネーム：<%= book.user.name %></p>
    <p>投稿ユーザー画像：<%= image_tag 'no_user.jpg' %></p>
  </div>
<% end %>