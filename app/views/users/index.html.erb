<h1>User 一覧</h1>
<% @users.each do |user| %>
  <div>
    <%= link_to user_path(user.id) do %>
      <%= image_tag user.get_profile_image %>
    <% end %>
    <p>投稿者：<%= user.name %></p>
  </div>
<% end %>